# App

This folder owns the startup scene and, later, the persistent application shell.
`foundation_preview.tscn` is intentionally static: it confirms the baseline
project settings and shared UI theme before the app shell is introduced.
